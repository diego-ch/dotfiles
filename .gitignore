# machine-local git config (see git/local.example, git/work.local.example)
local
work.local

# tpm clones plugins here if stow folds ~/.config/tmux into the repo
tmux/.config/tmux/plugins/

# editor / tooling scratch
.claude/
.DS_Store
